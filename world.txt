www.mldn.cnwww.mldn.cn
nihao
world
git config --global user.name 'canger'
git config --global user.email 'canger@qq.com'

git status

git add Hello.java
git commit -m "create new file Hello.java"

git add .
git commit -m "Add Two File"

git commit -a -m "change emp.java"

git diff world.txt

Q